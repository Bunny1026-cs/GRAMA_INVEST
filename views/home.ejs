<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Grama Invest</title>
    <style>
        
 * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Roboto', Arial, sans-serif;
    background-color: #0d1b2a; 
    color: #e0e1dd; 
    line-height: 1.6;
}


.navbar {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 0.6rem 2rem;
    background: linear-gradient(90deg, #57534e, #5c0d46); 
    color: #333;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
}

.navbar nav {
    display: flex;
    align-items: center;
    gap: 1.5rem;
}

.navbar a {
    color: #000;
    text-decoration: none;
    font-size: 1rem;
    font-weight: bold;
    position: relative;
    transition: color 0.3s ease;
}

.navbar a:hover {
    color: #333;
}

.hero {
    text-align: center;
    padding: rem 1rem;
    background: linear-gradient(90deg, #081a29, #696a77);
    color: #fcfaf2;
   
}

.hero h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
}

.hero p {
    font-size: 1.2rem;
}


.logo-container {
    margin: 1.5rem 0;
    text-align: center;
    
}

.logo-container img {
    max-width: 120px;
    height: auto;
    margin-bottom: 1rem;
}

.logo-container p {
    font-size: 1rem;
    color: #e0e1dd;
  
}

.buttons-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 2rem;
    padding: 2rem 1rem;
}

.button-card {
    background: #14213d; 
    border-radius: 10px;
    width: 280px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    text-align: center;
    padding: 1.5rem 1rem;
    position: relative;
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    color: #fff;
    border: 2px solid rgb(245, 240, 240); 
    max-width: 350px;
    flex: 1 1 30%;
    margin-bottom: 2rem;
    box-shadow: 0 0 10px rgba(234, 44, 164, 20);
}

.button-card img {
    max-width: 100%;
    height: 200px;
    object-fit: cover;
    border-radius: 10px;
}

.button-card h3 {
    margin-top: 1rem;
    font-size: 1.5rem;
    color: #ffeb3b;
}

.button-card p {
    margin-top: 0.5rem;
    font-size: 1rem;
}

.button-card:hover {
    transform: translateY(-10px);
    box-shadow: 0 8px 16px rgba(230, 224, 225, 0.7); 
}

.button-card:hover::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(221, 218, 218, 0.2); 
    z-index: -1;
    transition: all 0.3s ease-in-out;
}


.info {
    max-width: 800px;
    margin: 2rem auto;
    padding: 2rem;
    background: #1c2541;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    text-align: center;
    color: #e0e1dd;
}

.info p {
    font-size: 1rem;
}

.facts {
    padding: 2rem 1rem;
    background: #0d1b2a;
    color: #e0e1dd;
}

.facts h2 {
    text-align: center;
    margin-bottom: 1rem;
    font-size: 1.8rem;
    color: #ffeb3b;
}

.facts ul {
    max-width: 800px;
    margin: 0 auto;
    list-style: disc;
    padding-left: 1.5rem;
}

.facts li {
    margin-bottom: 0.8rem;
    font-size: 1rem;
}

.auth-buttons {
    position: absolute;
    top: 20px;
    right: 20px;
}

.auth-buttons button {
    margin: 0 5px;
    padding: 10px 20px;
    background: #444;
    border: none;
    color: white;
    cursor: pointer;
    border-radius: 5px;
    transition: background 0.3s, transform 0.3s;
}

.auth-buttons button:hover {
    background: #00ff00;
    transform: scale(1.1);
}

.modal {
    display: none;
    position: fixed;
    z-index: 1;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    overflow: auto;
    background-color: rgb(0,0,0);
    background-color: rgba(0,0,0,0.4);
    padding-top: 60px;
}

.modal-content {
    background-color: #fefefe;
    margin: 5% auto;
    padding: 20px;
    border: 1px solid #888;
    width: 80%;
    max-width: 500px;
    color: black;
    border-radius: 10px;
}

.close {
    color: #aaa;
    float: right;
    font-size: 28px;
    font-weight: bold;
}

.close:hover,
.close:focus {
    color: black;
    text-decoration: none;
    cursor: pointer;
}

.profile-section {
    position: absolute;
    top: 20px;
    right: -300px; /* Start off-screen */
    width: 250px;
    background: #444;
    color: white;
    border-radius: 5px;
    padding: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
    transition: right 0.3s ease-in-out;
}

.profile-section.show {
    right: 20px; /* Slide in */
}

.profile-section a {
    color: white;
    text-decoration: none;
    display: block;
    padding: 10px 0;
}

.profile-section a:hover {
    background: #00ff00;
}
    </style>
</head>
<body>

    <header class="navbar">
        <nav>
            <a href="#home">Home</a>
            <a href="#about">About</a>
            <a href="#contact">Contact Us</a>
        </nav>
        <div class="auth-buttons">
            <button onclick="showModal('signupModal')">Sign Up</button>
            <button onclick="showModal('loginModal')">Login</button>
            <button onclick="toggleProfileSection()">Profile</button>
        </div>
        <div id="profileSection" class="profile-section">
            <a href="#profile">View Profile</a>
            <a href="#settings">Settings</a>
            <a href="#logout">Logout</a>
        </div>
    </header>

    <section id="home" class="hero">
        <h1>Welcome to Grama Invest</h1>
        <p>Your guide to financial literacy and investment options.</p>
    </section>


    <div class="logo-container">
        <img src="gramainvest.jpg" alt="Grama Invest Logo">
        <p>Empowering rural India with financial knowledge.</p>
    </div>

  
    <section class="buttons-container">
        <div class="button-card">
            <img src="stock.jpg" alt="Stock Market">
            <h3>Stock Market</h3>
            <p>Learn about stock market investments and strategies.</p>
        </div>
        <div class="button-card">
            <img src="mutual fund.jpg" alt="Mutual Fund">
            <h3>Mutual Fund</h3>
            <p>Discover how mutual funds can grow your wealth.</p>
        </div>
        <div class="button-card">
            <img src="crypto.jpg" alt="Cryptocurrency">
            <h3>Cryptocurrency & Bitcoin</h3>
            <p>Explore the world of cryptocurrency and blockchain.</p>
        </div>
        <div class="button-card">
            <img src="govt.jpg" alt="Government Schemes">
            <h3>Govt. Investment Schemes</h3>
            <p>Understand government-backed savings options.</p>
        </div>
        <div class="button-card">
            <img src="fd & rd.jpg" alt="FD & RD">
            <h3>FD & RD</h3>
            <p>Learn the benefits of fixed and recurring deposits.</p>
        </div>
    </section>

    <section class="info">
        <p>A SEBI study reveals that only 27 per cent of the Indian population is financially literate, with even lower rates in rural areas and among women. Cultural and societal barriers significantly hinder many women from participating in financial decisions.</p>
    </section>

    <!-- Sign Up Modal -->
    <div id="signupModal" class="modal">
        <div class="modal-content">
            <span class="close" onclick="closeModal('signupModal')">&times;</span>
            <h2>Sign Up</h2>
            <form id="signupForm" method="POST" action="/signup">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" required><br><br>
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" required><br><br>
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" required><br><br>
                <button type="submit">Sign Up</button>
            </form>
        </div>
    </div>

    <!-- Login Modal -->
    <div id="loginModal" class="modal">
        <div class="modal-content">
            <span class="close" onclick="closeModal('loginModal')">&times;</span>
            <h2>Login</h2>
            <form id="loginForm" method="POST" action="/login">
                <label for="email">Email:</label>
                <input type="email" id="loginEmail" name="email" required><br><br>
                <label for="password">Password:</label>
                <input type="password" id="loginPassword" name="password" required><br><br>
                <button type="submit">Login</button>
            </form>
        </div>
    </div>

    <script>
        function showModal(modalId) {
            document.getElementById(modalId).style.display = "block";
        }

        function closeModal(modalId) {
            document.getElementById(modalId).style.display = "none";
        }

        function toggleProfileSection() {
            const profileSection = document.getElementById('profileSection');
            profileSection.classList.toggle('show');
        }

        window.onclick = function(event) {
            const profileSection = document.getElementById('profileSection');
            if (!profileSection.contains(event.target) && !event.target.matches('.auth-buttons button')) {
                profileSection.classList.remove('show');
            }
            if (event.target.classList.contains('modal')) {
                event.target.style.display = "none";
            }
        }

        document.getElementById('signupForm').onsubmit = async function(event) {
            event.preventDefault();
            const name = document.getElementById('name').value;
            const email = document.getElementById('email').value;
            const password = document.getElementById('password').value;

            try {
                const response = await fetch('/signup', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ name, email, password })
                });

                const data = await response.json();
                if (response.ok) {
                    alert('Sign up successful');
                    closeModal('signupModal');
                } else {
                    alert(data.message);
                }
            } catch (error) {
                console.error('Error during sign up:', error); // Log the error details
                alert('An error occurred');
            }
        }

        document.getElementById('loginForm').onsubmit = async function(event) {
            event.preventDefault();
            const email = document.getElementById('loginEmail').value;
            const password = document.getElementById('loginPassword').value;

            try {
                const response = await fetch('/login', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ email, password })
                });

                const data = await response.json();
                if (response.ok) {
                    alert('Login successful');
                    closeModal('loginModal');
                } else {
                    alert(data.message);
                }
            } catch (error) {
                alert('An error occurred');
            }
        }
    </script>
</body>
</html>